server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # This tells Nginx to let React handle the routing
        try_files $uri $uri/ /index.html;
    }

    # Proxy API requests to the backend
    location /api/ {
        # Resolver is REQUIRED when using variables in proxy_pass
        resolver 8.8.8.8 valid=30s;
        
        # Use envsubst to replace this placeholder at runtime
        set $backend_url ${BACKEND_URL}; 
        proxy_pass $backend_url;
        
        proxy_ssl_server_name on;
        proxy_ssl_verify off; # Ignore SSL verification issues for now to fix 502
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host ${BACKEND_HOST};
        proxy_cache_bypass $http_upgrade;
    }
}
